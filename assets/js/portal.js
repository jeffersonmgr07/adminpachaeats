const Portal={
  role:document.body.dataset.portalRole||'restaurant',base:'../',
  async data(){PE.base='../';return PE.load()},
  setup(){document.querySelector('[data-menu]')?.addEventListener('click',()=>document.querySelector('.portal-sidebar')?.classList.toggle('open'));document.querySelectorAll('[data-password-toggle]').forEach(btn=>btn.onclick=()=>{const input=btn.parentElement.querySelector('input');input.type=input.type==='password'?'text':'password';btn.textContent=input.type==='password'?'Ver':'Ocultar'})},
  restaurantLogin(e){e.preventDefault();const sel=document.querySelector('#restaurantDemoSelect');PE.setRestaurantDemoId(sel?.value||'qori-chicken');location.href='dashboard.html'},
  fakeLogin(e,target){e?.preventDefault();location.href=target},
  statusClass(s=''){return ['DELIVERED'].includes(s)?'green':['PREPARING','READY','PICKED_UP','ON_THE_WAY'].includes(s)?'yellow':['CANCELLED'].includes(s)?'red':'blue'},
  restaurantFrame(active,restaurant){return `<div class="portal-shell"><aside class="portal-sidebar"><a class="portal-logo" href="dashboard.html"><img src="../assets/img/logos/Logo1.png" alt=""><span>Pacha Eats</span></a><div class="portal-account-card"><small>Negocio demo</small><strong>${PE.escape(restaurant?.name||'Restaurante')}</strong><div class="store-state">● Operando ahora</div></div><nav class="portal-nav"><a class="${active==='dashboard'?'active':''}" href="dashboard.html"><span class="nav-icon">▦</span>Resumen</a><a class="${active==='orders'?'active':''}" href="pedidos.html"><span class="nav-icon">▤</span>Pedidos</a><a href="menu.html"><span class="nav-icon">☰</span>Menú</a><a href="promociones.html"><span class="nav-icon">%</span>Promociones</a><a href="ventas.html"><span class="nav-icon">↗</span>Ventas</a></nav><div class="portal-sidebar-bottom"><a href="../index.html">↗ Ver Pacha Eats</a><a href="index.html">⇄ Cambiar restaurante demo</a></div></aside><main class="portal-main"><header class="portal-topbar"><button class="portal-mobile-menu" data-menu>☰</button><div class="portal-page-title"><small>Portal de comercio</small><strong>${active==='orders'?'Pedidos':'Resumen'}</strong></div><div class="portal-top-actions"><span class="live-chip">● Demo en vivo</span><div class="portal-avatar">${(restaurant?.name||'PE').split(' ').map(x=>x[0]).slice(0,2).join('')}</div></div></header><div class="portal-content" id="portalContent"></div></main></div>`},
  driverFrame(active){return `<div class="portal-shell"><aside class="portal-sidebar"><a class="portal-logo" href="dashboard.html"><img src="../assets/img/logos/Logo1.png" alt=""><span>Pacha Eats</span></a><div class="portal-account-card"><small>Repartidor demo</small><strong>Diego R. · Moto</strong><div class="store-state">● Disponible</div></div><nav class="portal-nav"><a class="${active==='dashboard'?'active':''}" href="dashboard.html"><span class="nav-icon">▦</span>Inicio</a><a class="${active==='orders'?'active':''}" href="pedidos.html"><span class="nav-icon">🛵</span>Entregas</a><a href="ganancias.html"><span class="nav-icon">S/</span>Ganancias</a><a href="documentos.html"><span class="nav-icon">▤</span>Documentos</a></nav><div class="portal-sidebar-bottom"><a href="../index.html">↗ Ver Pacha Eats</a><a href="index.html">⇥ Cerrar sesión</a></div></aside><main class="portal-main"><header class="portal-topbar"><button class="portal-mobile-menu" data-menu>☰</button><div class="portal-page-title"><small>Portal de repartidor</small><strong>${active==='orders'?'Entregas':'Inicio'}</strong></div><div class="portal-top-actions"><span class="live-chip">● Disponible</span><div class="portal-avatar">DR</div></div></header><div class="portal-content" id="portalContent"></div></main></div>`},
  orderItems(order){return (order.items||[]).map(i=>`<div class="portal-order-item"><div><strong>${i.qty}× ${PE.escape(i.name)}</strong>${i.modifiers?.length?`<small>${i.modifiers.map(m=>PE.escape(m.name)).join(' · ')}</small>`:''}${i.note?`<small>Nota: ${PE.escape(i.note)}</small>`:''}</div><b>${PE.money(i.lineTotal)}</b></div>`).join('')||'<small>Detalle no disponible.</small>'},
  restaurantAction(order){if(order.status==='PENDING_RESTAURANT')return `<button class="portal-btn primary" onclick="Portal.setRestaurantStatus('${order.id}','ACCEPTED')">Aceptar pedido</button>`;if(order.status==='ACCEPTED')return `<button class="portal-btn primary" onclick="Portal.setRestaurantStatus('${order.id}','PREPARING')">Iniciar preparación</button>`;if(order.status==='PREPARING')return `<button class="portal-btn primary" onclick="Portal.setRestaurantStatus('${order.id}','READY')">Marcar listo</button>`;if(order.status==='READY')return `<span class="portal-badge yellow">Buscando repartidor</span>`;if(['DRIVER_ASSIGNED','PICKED_UP','ON_THE_WAY'].includes(order.status))return `<span class="portal-badge blue">En manos del repartidor</span>`;if(order.status==='DELIVERED')return `<span class="portal-badge green">Completado</span>`;return ''},
  restaurantOrderCard(o){return `<article class="workflow-order-card"><div class="workflow-order-head"><div><span class="order-code">${PE.escape(o.id)}</span><h3>${PE.escape(o.customer)}</h3><small>${new Date(o.createdAt).toLocaleString('es-PE')}</small></div><div><span class="portal-badge ${this.statusClass(o.status)}">${PE.orderStatusLabel(o.status)}</span><strong class="workflow-total">${PE.money(o.total)}</strong></div></div><div class="workflow-order-grid"><div><small>Entrega</small><strong>${PE.escape(o.address)}</strong><span>${PE.escape(o.reference||'Sin referencia')}</span></div><div><small>Pago</small><strong>${PE.paymentStatusLabel(o.paymentStatus)}</strong><span>${PE.escape(o.paymentMethod)}</span></div><div><small>Repartidor</small><strong>${PE.escape(o.rider||'Sin asignar')}</strong><span>${o.status==='READY'?'Pedido visible para repartidores':'—'}</span></div></div><div class="portal-order-items">${this.orderItems(o)}</div><div class="workflow-order-actions">${this.restaurantAction(o)}<a class="portal-btn outline" href="../seguimiento-pedido.html?id=${encodeURIComponent(o.id)}" target="_blank">Ver seguimiento ↗</a></div></article>`},
  async renderRestaurantPage(active='dashboard'){await this.data();const restaurant=PE.getRestaurant(PE.restaurantDemoId())||PE.data.restaurants[0];document.querySelector('#portalFrame').innerHTML=this.restaurantFrame(active,restaurant);this.setup();const render=()=>{const orders=PE.ordersForRestaurant(restaurant.id);const content=document.querySelector('#portalContent');if(active==='dashboard'){const pending=orders.filter(o=>['PENDING_RESTAURANT','ACCEPTED','PREPARING'].includes(o.status)).length,ready=orders.filter(o=>o.status==='READY').length,done=orders.filter(o=>o.status==='DELIVERED').length,sales=orders.filter(o=>o.status==='DELIVERED').reduce((s,o)=>s+o.total,0);content.innerHTML=`<div class="portal-greeting"><div><h1>${PE.escape(restaurant.name)}</h1><p>Operación demo conectada con la web del cliente y el portal del repartidor.</p></div><a class="portal-btn outline" href="../restaurante.html?id=${encodeURIComponent(restaurant.id)}" target="_blank">Ver mi tienda ↗</a></div><section class="portal-kpis"><article class="metric-card"><div class="metric-head"><span>Pedidos activos</span><span class="metric-icon">▤</span></div><strong>${pending}</strong><span class="metric-trend">requieren atención</span></article><article class="metric-card"><div class="metric-head"><span>Listos</span><span class="metric-icon">✓</span></div><strong>${ready}</strong><span class="metric-trend">esperando rider</span></article><article class="metric-card"><div class="metric-head"><span>Entregados</span><span class="metric-icon">🛵</span></div><strong>${done}</strong><span class="metric-trend muted">pedidos demo</span></article><article class="metric-card"><div class="metric-head"><span>Ventas entregadas</span><span class="metric-icon">S/</span></div><strong>${PE.money(sales)}</strong><span class="metric-trend">demo local</span></article></section><section class="portal-card workflow-section"><div class="portal-card-header"><div><h2>Pedidos recientes</h2><p>Los pedidos creados por clientes aparecen aquí automáticamente.</p></div><a class="portal-link" href="pedidos.html">Gestionar todos →</a></div><div class="workflow-order-list">${orders.slice(0,4).map(o=>this.restaurantOrderCard(o)).join('')||this.emptyRestaurant(restaurant)}</div></section>`}else{content.innerHTML=`<div class="portal-greeting"><div><h1>Pedidos de ${PE.escape(restaurant.name)}</h1><p>Acepta, prepara y marca como listo. Cuando esté listo, el pedido aparecerá en el portal del repartidor.</p></div><span class="live-chip">${orders.length} pedidos demo</span></div><div class="workflow-filter-row"><button class="is-active">Todos</button><span>Flujo: recibido → aceptado → preparando → listo</span></div><div class="workflow-order-list">${orders.map(o=>this.restaurantOrderCard(o)).join('')||this.emptyRestaurant(restaurant)}</div>`}};render();window.addEventListener('pe:orders',render);window.addEventListener('storage',e=>{if(e.key===PE.ORDER_KEY)render()})},
  emptyRestaurant(r){return `<div class="portal-empty workflow-empty"><div>▤</div><h3>Aún no hay pedidos para ${PE.escape(r.name)}</h3><p>Abre la tienda pública, agrega un producto y finaliza el checkout para probar el circuito.</p><a class="portal-btn primary" href="../restaurante.html?id=${encodeURIComponent(r.id)}" target="_blank">Crear pedido de prueba ↗</a></div>`},
  setRestaurantStatus(id,status){const order=PE.transitionOrder(id,status,'RESTAURANT');if(status==='READY')PE.updateOrder(id,{rider:'',riderId:''});return order},
  driverOfferCard(o){return `<article class="driver-offer-card"><div class="driver-offer-head"><div><span class="portal-badge red">Nueva oferta</span><h3>${PE.escape(o.restaurant)}</h3><p>${PE.escape(o.restaurantAddress||'Pachacámac')} → ${PE.escape(o.address)}</p></div><strong>${PE.money(Math.max(6.5,Number(o.delivery||0)))}</strong></div><div class="offer-meta"><div><strong>${Number(o.distanceKm||3.9).toFixed(1)} km*</strong><small>Distancia demo</small></div><div><strong>20–30 min</strong><small>Estimado</small></div><div><strong>${PE.money(o.total)}</strong><small>Valor pedido</small></div></div><div class="offer-actions"><button class="portal-btn outline" type="button">Omitir</button><button class="portal-btn primary" onclick="Portal.acceptDriverOrder('${o.id}')">Aceptar entrega</button></div></article>`},
  driverActiveCard(o){let action='';if(o.status==='DRIVER_ASSIGNED')action=`<button class="portal-btn primary" onclick="Portal.driverStatus('${o.id}','PICKED_UP')">Confirmar recojo</button>`;else if(o.status==='PICKED_UP')action=`<button class="portal-btn primary" onclick="Portal.driverStatus('${o.id}','ON_THE_WAY')">Iniciar entrega</button>`;else if(o.status==='ON_THE_WAY')action=`<div class="delivery-pin-confirm"><input inputmode="numeric" maxlength="4" placeholder="PIN cliente" id="pin-${o.id}"><button class="portal-btn primary" onclick="Portal.completeWithPin('${o.id}')">Confirmar entrega</button></div>`;return `<article class="driver-active-card"><div class="workflow-order-head"><div><span class="order-code">${o.id}</span><h3>${PE.escape(o.restaurant)} → ${PE.escape(o.customer)}</h3><small>${PE.orderStatusLabel(o.status)}</small></div><span class="portal-badge ${this.statusClass(o.status)}">Activa</span></div><div class="driver-route-card"><div><small>Recojo</small><strong>${PE.escape(o.restaurantAddress||o.restaurant)}</strong></div><span>↓</span><div><small>Entrega</small><strong>${PE.escape(o.address)}</strong><p>${PE.escape(o.reference||'')}</p></div></div><div class="workflow-order-actions">${action}<a class="portal-btn outline" href="../seguimiento-pedido.html?id=${encodeURIComponent(o.id)}" target="_blank">Ver tracking ↗</a></div></article>`},
  async renderDriverPage(active='dashboard'){await this.data();document.querySelector('#portalFrame').innerHTML=this.driverFrame(active);this.setup();const render=()=>{const offers=PE.availableDriverOrders(),activeOrders=PE.activeDriverOrders(),completed=PE.completedDriverOrders();const earnings=completed.reduce((s,o)=>s+Math.max(6.5,Number(o.delivery||0)),0);const content=document.querySelector('#portalContent');if(active==='dashboard'){content.innerHTML=`<div class="portal-greeting"><div><h1>Hola, Diego 👋</h1><p>Los pedidos que el restaurante marca como listos aparecen aquí como ofertas.</p></div><div class="status-control"><span>Disponible</span><button class="toggle on"></button></div></div><section class="portal-kpis"><article class="metric-card"><div class="metric-head"><span>Ofertas</span><span class="metric-icon">◎</span></div><strong>${offers.length}</strong><span class="metric-trend">disponibles ahora</span></article><article class="metric-card"><div class="metric-head"><span>Entregas activas</span><span class="metric-icon">🛵</span></div><strong>${activeOrders.length}</strong><span class="metric-trend">en curso</span></article><article class="metric-card"><div class="metric-head"><span>Completadas</span><span class="metric-icon">✓</span></div><strong>${completed.length}</strong><span class="metric-trend muted">en esta demo</span></article><article class="metric-card"><div class="metric-head"><span>Ganancias</span><span class="metric-icon">S/</span></div><strong>${PE.money(earnings)}</strong><span class="metric-trend">entregas demo</span></article></section><section class="portal-grid-2 workflow-section"><article class="portal-card"><div class="portal-card-header"><div><h2>Ofertas disponibles</h2><p>Solo pedidos listos y sin repartidor.</p></div></div><div class="driver-offer-list">${offers.map(o=>this.driverOfferCard(o)).join('')||'<div class="portal-empty">No hay ofertas por ahora. Marca un pedido como listo desde el restaurante.</div>'}</div></article><article class="portal-card"><div class="portal-card-header"><div><h2>Entrega activa</h2><p>Avanza el estado de tu entrega.</p></div></div>${activeOrders.map(o=>this.driverActiveCard(o)).join('')||'<div class="portal-empty">No tienes una entrega activa.</div>'}</article></section>`}else{content.innerHTML=`<div class="portal-greeting"><div><h1>Entregas</h1><p>Gestiona ofertas, pedidos activos e historial de entregas.</p></div></div><section class="portal-card workflow-section"><div class="portal-card-header"><div><h2>Ofertas</h2><p>Pedidos listos para recojo</p></div><span class="portal-badge red">${offers.length}</span></div><div class="driver-offer-list">${offers.map(o=>this.driverOfferCard(o)).join('')||'<div class="portal-empty">Sin ofertas disponibles.</div>'}</div></section><section class="portal-card workflow-section"><div class="portal-card-header"><div><h2>Activas</h2><p>Entregas asignadas a ti</p></div></div>${activeOrders.map(o=>this.driverActiveCard(o)).join('')||'<div class="portal-empty">No tienes entregas activas.</div>'}</section><section class="portal-card workflow-section"><div class="portal-card-header"><div><h2>Historial</h2><p>Entregas completadas</p></div></div><div class="compact-history">${completed.map(o=>`<div><strong>${o.id}</strong><span>${PE.escape(o.restaurant)} → ${PE.escape(o.customer)}</span><b>${PE.money(Math.max(6.5,Number(o.delivery||0)))}</b></div>`).join('')||'<div class="portal-empty">Aún no hay entregas completadas.</div>'}</div></section>`}};render();window.addEventListener('pe:orders',render);window.addEventListener('storage',e=>{if(e.key===PE.ORDER_KEY)render()})},
  acceptDriverOrder(id){const current=PE.getOrder(id);if(!current||current.status!=='READY'||current.riderId)return;PE.transitionOrder(id,'DRIVER_ASSIGNED','DRIVER',{rider:PE.RIDER_NAME,riderId:'demo-rider'})},
  driverStatus(id,status){PE.transitionOrder(id,status,'DRIVER')},
  completeWithPin(id){const input=document.querySelector(`#pin-${id}`),order=PE.getOrder(id);if(!order)return;if((input?.value||'').trim()!==String(order.deliveryPin)){input?.classList.add('input-error');if(input)input.placeholder='PIN incorrecto';return}PE.transitionOrder(id,'DELIVERED','DRIVER');}
};
Portal.fillTable=async function(type){this.setup();await this.data();const table=document.querySelector('#portalTable');if(!table)return;if(type==='products'){const rid=PE.restaurantDemoId();const rows=(PE.data.products||[]).filter(p=>p.restaurantId===rid);table.innerHTML='<thead><tr><th>Producto</th><th>Categoría</th><th>Precio</th><th>Disponibilidad</th><th>Opciones</th></tr></thead><tbody>'+rows.map(p=>`<tr><td><div class="product-cell"><img class="product-thumb" src="../${p.image}" alt=""><strong>${PE.escape(p.name)}</strong></div></td><td>${PE.escape(p.category)}</td><td><strong>${PE.money(p.price)}</strong></td><td><span class="portal-badge ${p.available?'green':'red'}">${p.available?'Disponible':'No disponible'}</span></td><td>${p.modifierGroups?.length?`${p.modifierGroups.length} grupos de extras`:'Sin extras'}</td></tr>`).join('')+'</tbody>'}}
window.Portal=Portal;document.addEventListener('DOMContentLoaded',()=>Portal.setup());
